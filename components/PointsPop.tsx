"use client";

export function triggerPoints(_delta: number, _label?: string) {
  // XP popups were retired with the rewards economy. Keep this shim so older
  // completion paths can keep calling it until those files are simplified.
}

export function PointsPop() {
  return null;
}
